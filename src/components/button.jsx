const Button=(props)=> {

        return (
            <div>
              <button className="btn btn-outline-success">{props.name}</button>  
            </div>

        )

}

export default Button;
